## Agent 技能

### 问题跟踪器（Issue tracker）

问题以 Markdown 文件的形式存放在 `.scratch/<feature-slug>/` 下。参见 `docs/agents/issue-tracker.md`。

### 分流标签（Triage labels）

使用默认标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix`。参见 `docs/agents/triage-labels.md`。

### 领域文档（Domain docs）

这是一个单上下文仓库：在相关工作中读取根目录下的 `CONTEXT.md` 和 `docs/adr/`。参见 `docs/agents/domain.md`。
